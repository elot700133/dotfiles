
alias ls='ls --color=auto'
alias suspend='sudo systemctl suspend'
#alias eboot='sudo reboot'
alias turnoff='sudo shutdown -h +0'
alias vi='vim'
alias pdfr='apvlv'
alias ls='ls -h --color=auto'
alias ll='ls -l'
alias la='ls -al'
alias cd..='cd ..'
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'
alias h="history"
alias hg="history | grep"
alias p="ps -ef | grep"
alias f="find . |grep" #
alias cal="cal `date +%Y`"
alias exm="cd ~/.xmonad; vi xmonad.hs"
alias c="clear"
alias giteye='GitEye'
alias tarz='tar -zxvf'
alias urxvt='urxvt -bg Black -fg White'
alias term='urxvt'
alias dir-usage='du --summarize --human-readable'
alias disk-usage='df --human-readable'
# check for failure
# systemctl --failed

#--------------------------------------------
# Pacman alias
#--------------------------------------------
alias pacupg='sudo pacman -Syu'        # Synchronize with repositories before upgrading packages that are out of date on the local system.
alias pacin='sudo pacman -S'           # Install specific package(s) from the repositories
alias pacins='sudo pacman -U'          # Install specific package not from the repositories but from a file 
alias pacre='sudo pacman -R'           # Remove the specified package(s), retaining its configuration(s) and required dependencies
alias pacrem='sudo pacman -Rns'        # Remove the specified package(s), its configuration(s) and unneeded dependencies
alias pacrep='pacman -Si'              # Display information about a given package in the repositories
alias pacsrh='pacman -Ss'              # Search for package(s) in the repositories
alias pacloc='pacman -Qi'              # Display information about a given package in the local database
alias paclocs='pacman -Qs'             # Search for package(s) in the local database
# Additional pacman alias examples
alias pacupd='sudo pacman -Sy && sudo abs'     # Update and refresh the local package and ABS databases against repositories
alias pacinsd='sudo pacman -S --asdeps'        # Install given package(s) as dependencies of another package
alias pacmir='sudo pacman -Syy'                # Force refresh of all package lists after updating /etc/pacman.d/mirrorlist

# pacman -Qqm                          # list foreign packages
# pacman -Qqe                          # list explicitly installed packages
# reinstall all foreign and explicitly packages
# pacman -Qqe | grep -v "$(pacman -Qqm)" > pacman.lst
# then
# cat pacman.lst | xargs pacman -S --needed --noconfirm

alias yaoupd='yaourt -Syua --noconfirm'
